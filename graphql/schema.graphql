type Person {
    id: Int!
    name: String!
    age: Int!
    gender: String!
}

type Query {
    people: [Person]!
    person(id: Int!): Person!
}

# 1) Query는 정보 받을 때만 쓰임
# 2) Mutation(변형): 정보를 바꿀 때